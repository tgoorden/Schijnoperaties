---
layout: base.njk
title: Place of origin
---
<section class="page-heading" aria-labelledby="page-title">
  <p class="eyebrow">Subcollections</p>
  <h1 id="page-title">Place of origin</h1>
</section>
<ol class="taxonomy-list">
{% for key in lookups.locations | keysByLabel %}
  {% set label = key | labelFor(lookups.locations) %}
  {% set itemCount = collections.metadata | taxonomyCount(key, ['place_creation', 'location']) %}
  <li><a href="/locations/{{ key | slug }}/"><span>{{ label }}</span><small>{{ itemCount }}</small></a></li>
{% endfor %}
</ol>
